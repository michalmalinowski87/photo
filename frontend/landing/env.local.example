## Landing (website) - local development
#
# Copy to `.env.local` (not committed):
#   cp env.local.example .env.local
#
# No fallbacks: all values are required.

# For local development with Traefik proxy, use lvh.me domains:
NEXT_PUBLIC_LANDING_URL=https://photocloud.lvh.me
NEXT_PUBLIC_DASHBOARD_URL=https://dashboard.lvh.me
# API URL (used for /config – company data for legal pages). Optional; if unset, legal pages use TBA.
# NEXT_PUBLIC_API_URL=https://api.lvh.me
# For direct localhost access (without proxy), use:
# NEXT_PUBLIC_LANDING_URL=http://localhost:3002
# NEXT_PUBLIC_DASHBOARD_URL=http://localhost:3000
# NEXT_PUBLIC_API_URL=http://localhost:3001

