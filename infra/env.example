STAGE=dev
AWS_REGION=eu-west-1
AWS_PROFILE=dev
CDK_DEFAULT_ACCOUNT=
CDK_DEFAULT_REGION=eu-west-1

SENDER_EMAIL=
PHOTOGRAPHER_NOTIFY_EMAIL=

# Required (for payments)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Required (public URLs written to SSM)
# Your deployed API Gateway URL
PUBLIC_API_URL=

# Frontend URLs (used for emails, redirects, and links sent by backend)
# For local development with Traefik proxy, use lvh.me domains:
PUBLIC_GALLERY_URL=https://gallery.lvh.me
PUBLIC_LANDING_URL=https://photocloud.lvh.me
PUBLIC_DASHBOARD_URL=https://dashboard.lvh.me

# For direct localhost access (without proxy), use:
# PUBLIC_GALLERY_URL=http://localhost:3001
# PUBLIC_LANDING_URL=http://localhost:3002
# PUBLIC_DASHBOARD_URL=http://localhost:3000

# For deployed environments (dev/staging/prod), use your actual domain URLs


GALLERY_PASSWORD_ENCRYPTION_SECRET=